import React from 'react';

const Hero = (props) =>{
    return (
        <div className={'hero'}>
            <div className={'flex-1 pt-36'}>
                <h1 className={'hero__title'}>
                    {'Hero'}
                </h1>
            </div>
        </div>
    );
};

export default Hero;
